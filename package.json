{
  "name": "desafio-cypress-parana-banco",
  "version": "1.0.0",
  "scripts": {
    "cypress:e2e": "cypress open --browser chrome",
    "cypress:headless": "cypress run --browser chrome"
  },
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^18.0.2",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.0",
    "chai-json-schema": "^1.5.1",
    "cypress": "^12.17.2"
  },
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": "cypress/support/steps/*.{js,ts}"
  }
}
